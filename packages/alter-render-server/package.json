{
  "name": "@alter/render-server",
  "version": "0.1.0",
  "description": "Mountable Express router for the Alter before/after render engine. Keeps provider API keys server-side.",
  "main": "src/index.js",
  "files": [
    "src",
    "README.md"
  ],
  "peerDependencies": {
    "express": ">=4"
  },
  "dependencies": {
    "@alter/render-core": "0.1.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "express": "^4.18.2",
    "supertest": "^6.3.3"
  }
}
